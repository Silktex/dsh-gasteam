/** Pure host half for the browser-discovered GasView visual agents UI plugin. */

/** Host plugin body; visual behavior lives in the browser export consuming the Team Remote. */
export function apply(): void {}
